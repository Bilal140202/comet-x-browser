<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- v1.7.0 background model downloads: foreground service (dataSync) keeps
         the download alive while the app is closed; notifications are optional
         (download proceeds even if POST_NOTIFICATIONS is denied on 13+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- v1.8.0 background agent: the headless task engine must not stall in
         deep sleep mid-task; the lock is renewed per step and released at
         every pause/terminal state. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".CometApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.CometX">

        <!-- WorkManager's foreground service runs our model downloads: it must
             be allowed the dataSync type on API 34+ (merged, not replaced). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- v1.8.0: background agent engine. NOT exported (only the app and its
             own notification PendingIntents may reach it); START_NOT_STICKY —
             the system never resurrects it with a null goal, so a reboot can
             never crash-loop the app ("App keep closing" contract BG-7). -->
        <service
            android:name=".background.AgentTaskService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:configChanges="uiMode"
            android:label="@string/settings_title"
            android:parentActivityName=".ui.MainActivity" />
    </application>
</manifest>
